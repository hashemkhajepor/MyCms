@using System.Configuration;
@model DataLayer.Users

<div style="padding:20px" dir="rtl">
    <h3>@Model.UserName عزیز!</h3>
    <p>با تشکر از شما، جهت فعال سازی حساب کاربری خود روی لینک زیر کلیک کنید</p>
    <a href="@ConfigurationManager.AppSettings["MyDomain"]/Account/ActiveUser/@Model.ActiveCode">لینک فعال سازی</a>
</div>

